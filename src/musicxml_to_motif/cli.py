"""Minimal CLI for musicxml-to-motif."""

from musicxml_to_motif import __version__


def main() -> None:
    """Print a simple package greeting."""
    print(f"musicxml-to-motif package skeleton (version {__version__})")
